import styled from "styled-components";
import { mobileView } from "./helpers/resposive-views";
export const HeaderWrapper = styled.div`
  margin: 1.62rem 1.64rem 0 1.5rem;
  max-width: ${mobileView};
`;
